apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-console.fullname" . }}
  labels:
    {{- include "kube-console.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-console.selectorLabels" . | nindent 4 }}
